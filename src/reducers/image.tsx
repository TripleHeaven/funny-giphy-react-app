import { FETCH_IMAGE_SUCCES } from "../actions/actionTypes";

import * as R from "ramda";

const initialState = {};

// export default (state = initialState, { type, payload }) => {
//   switch (type) {
//     case FETCH_IMAGE_SUCCES:
//       const newValues = payload;
//       return newValues;
//     default:
//       return null;
//   }
// };
